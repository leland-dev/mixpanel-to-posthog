# migrate mixpanel to posthog

This is a rewrite of this repository in Python. Note this only supports migrating events, not users, projects, etc.

https://github.com/stablecog/mixpanel-to-posthog

### getting started
 Clone the repo, create venv, install dependencies, and run `python main.py`